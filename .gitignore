.superpowers/
.DS_Store
build/
.build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
.swiftpm/
**/xcuserdata/

# Signing and notarization secrets. These must never enter the repo — an
# App Store Connect private key downloads exactly once and grants API access
# to the whole team account. Keep them in ~/.private/fulcrum/ instead.
*.p8
*.p12
*.cer
*.mobileprovision
AuthKey_*

# CDK build output for the download site
site/infra/node_modules/
site/infra/cdk.out/
