/// Namespace for package-wide constants.
public enum FulcrumKit {
    /// The tilt version Fulcrum's API expectations were verified against.
    /// Older versions are not blocked, but warn — `UIResource` shape and
    /// watch behaviour are unverified below this.
    public static let minimumTiltVersion = "0.36.3"
}
